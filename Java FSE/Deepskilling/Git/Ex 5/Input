cd GitDemo
Verify if main is in clean state:
git checkout main
git status
List all available branches:
git branch -a
Pull latest changes from remote:
git pull origin main
Push your local commits  to remote:
git push origin main
